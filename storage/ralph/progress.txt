## Codebase Patterns

- **Document model**: `app/Models/Document.php` — SFC pattern with fillable, casts(), scopes
- **DocumentFactory**: `database/factories/DocumentFactory.php` — states: prd(), spec(), decision(), reference(), pinned(), context(), forProject()
- **MCP Tools de documento**: `app/Mcp/Tools/` — ListDocumentsTool, GetDocumentTool, CreateDocumentTool, UpdateDocumentTool
- **Testes pré-existentes com falhas**: AiInsightsTest (5), AiIntegrationTest (1), AnalyticsPageTest (3), DocumentIntegrationTest (1) — não relacionados às mudanças de documentos

---

## 2026-02-26 - US-5
- Migration `add_is_context_to_documents_table`: coluna `is_context` boolean default false, com índice
- Model Document: adicionado `is_context` ao $fillable, cast boolean, scope `context()`
- Factory DocumentFactory: adicionado state `context()`
- Files changed:
  - database/migrations/2026_02_26_212531_add_is_context_to_documents_table.php (new)
  - app/Models/Document.php
  - database/factories/DocumentFactory.php
- **Learnings for future iterations:**
  - O banco local está vazio (sem tasks) — as tasks do SoloBoard estão na instância remota
  - 10 testes pré-existentes falhando (AI, Analytics, DocumentIntegration) — não relacionados
  - Padrão de scope no Document: `scopeXxx(Builder $query): void`
  - Factory states seguem padrão: `->state(fn (array $attributes) => [...])`
---

## 2026-02-26 - US-6
- ListDocumentsTool atualizado: filtro padrão `is_context=true`, parâmetro `include_all`
- Conteúdo truncado: primeiros 255 + últimos 255 chars quando > 510
- Campo `is_context` incluído no mapeamento, `excerpt` substituído por `content_preview`
- 3 testes existentes adaptados para usar `->context()` factory state
- Files changed:
  - app/Mcp/Tools/ListDocumentsTool.php
  - tests/Feature/Mcp/DocumentToolsTest.php
- **Learnings for future iterations:**
  - Ao mudar filtro padrão de query (ex: `context()`), testes existentes quebram — precisam ser adaptados
  - `Str::length()` / `Str::substr()` são preferíveis a `strlen()` / `substr()` para suporte Unicode
  - `$schema->boolean()` funciona para parâmetros MCP (confirmado em ToggleRecurringTaskTool, StartTimerTool)
  - Campo renomeado de `excerpt` para `content_preview` para refletir o novo comportamento de truncamento
---
