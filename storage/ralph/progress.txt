## Codebase Patterns

- **Sidebar**: `resources/views/layouts/app/sidebar.blade.php` — grupos: Visão Geral, Planejamento (Work, Hoje, Semana), Acompanhamento, Análise
- **Rotas**: `routes/web.php` — usa `Route::livewire()` com middleware `auth`
- **Features page**: `resources/views/pages/⚡features.blade.php` — SFC Livewire com Kanban
- **Testes Features**: `tests/Feature/FeaturesKanbanTest.php` — usa `route('work')` após renomeação
- **Keyboard shortcuts**: `resources/views/layouts/app.blade.php` — Alpine.js x-on:keydown.window
- **Command Palette**: `resources/views/components/⚡command-palette.blade.php` — referencia shortcuts inline
- **Redirect backward compat**: `Route::redirect('/features', '/work')` adicionado em web.php
- **Table sorting pattern**: `$sortBy`/`$sortDirection` com `#[Url]`, `sort()` method, `sortedFeatures()` computed que ordena collection em memória

---

## 2026-02-26 - US-928
- Renomeado label "Features" para "Work" na sidebar com ícone `briefcase`
- Atualizada rota `/features` → `/work` com nome `work`
- Adicionado redirect `/features` → `/work` para backward compatibility
- Atualizado heading da página de "Features" para "Work"
- Atualizado FeaturesKanbanTest: `route('features')` → `route('work')` em todas as ocorrências
- Files changed: sidebar.blade.php, routes/web.php, ⚡features.blade.php, FeaturesKanbanTest.php
- **Learnings for future iterations:**
  - `replace_all` no Edit tool só substitui a string exata — `route('features')` ≠ `route('features', [...])` — precisa tratar variações manualmente
  - 10 testes pré-existentes falhando (AiInsightsTest, AiIntegrationTest, AnalyticsPageTest, DocumentIntegrationTest) — não relacionados a esta mudança
  - O atalho `Ctrl+F` em app.blade.php navega para `/features` (agora redireciona) — será atualizado em US-932
  - Command palette referencia "Features" — será atualizado em US-932
---

## 2026-02-26 - US-929
- Removidos itens "Caixa de Entrada" (inbox) e "Kanban" (kanban de tasks) da sidebar
- Rotas /inbox e /kanban mantidas — apenas escondidas da navegação
- Files changed: sidebar.blade.php
- **Learnings for future iterations:**
  - Mudança simples — apenas remoção de linhas na sidebar, sem impacto em testes existentes
  - 19 testes de FeaturesKanban continuam passando — sidebar não é testada diretamente nesses testes
---

## 2026-02-26 - US-930
- Adicionada property `viewMode` com `#[Url(as: 'view')]` (default: 'kanban')
- Toggle segmented com `<flux:radio.group variant="segmented">` e ícones `view-columns`/`list-bullet`
- Kanban board condicionado a `$viewMode === 'kanban'`
- Placeholder para Table view com empty state (será implementado em US-931)
- View mode persiste na URL como `?view=kanban` ou `?view=table`
- Files changed: ⚡features.blade.php
- **Learnings for future iterations:**
  - `<flux:radio.group variant="segmented" size="sm">` com `<flux:radio icon="..." />` (sem label) cria toggle compacto icon-only
  - `#[Url(as: 'view')]` permite alias customizado no query string — importante para URLs limpas
  - 19 testes existentes continuam passando — o viewMode default 'kanban' garante backward compat
---

## 2026-02-26 - US-931
- Implementada visualização Table/List completa com `<flux:table>`
- 8 colunas: ID (#F-{id}), Título, Status (badge colorido), Prioridade (ícone+label), Projeto (emoji+nome), Progresso (barra %), Tempo Total, Due Date
- Sorting por 7 colunas (todas exceto Projeto) via `sortBy`/`sortDirection` com `#[Url]`
- Computed property `sortedFeatures()` ordena collection em memória (suporta campos computados: status, progress, total_time)
- Mesmos filtros do Kanban (projeto, prioridade, overdue) funcionam automaticamente pois ambas views usam `$this->features`
- Rows clicáveis abrem `open-feature-modal` — mesma interação do Kanban card
- Empty state quando nenhuma feature encontrada
- Due date com destaque vermelho para vencidas
- Files changed: ⚡features.blade.php
- **Learnings for future iterations:**
  - `flux:table.column` aceita `sortable`, `:sorted`, `:direction`, `wire:click` para sorting completo
  - `flux:badge` com `inset="top bottom"` cria badges compactos sem padding vertical excessivo (ideal para tabelas)
  - `flux:table.row` aceita `wire:click` para tornar rows clicáveis
  - Sorting de computed attributes (status, progress, total_time) exige sortBy na collection, não na query — `$collection->sortBy()` com closure funciona bem
  - `$this->sortedFeatures` precisa ser limpo em `refreshBoard()` junto com `$this->features`
---
