## Codebase Patterns

- **SFC pattern**: Todos componentes Livewire são Single-File Components com `@php new class extends Component { } @endphp`
- **Kanban columns**: features usam `FeatureStatus` (Draft, Backlog, Todo, Doing, Done); tasks usam `TaskStatus` (Inbox, Backlog, Todo, Doing, Done)
- **Feature status é computed**: baseado nas tasks (todas done = Done, alguma doing = Doing, etc.)
- **Done column**: filtrar por `completed_at` na semana corrente para tasks
- **Imports Livewire**: `use Livewire\Attributes\Url;` para `#[Url]`, `use Livewire\Attributes\Computed;` para `#[Computed]`
- **Limits pattern**: array `$limits` com status como key, `loadMore()` incrementa 20
- **Refresh pattern**: `unset($this->computedProp)` para invalidar computed properties
- **Feature card component**: `resources/views/components/feature-card.blade.php` — Blade component com @props
- **Project-detail tabs**: board, features, docs, metrics — board tab contém kanban de features
- **Pre-existing test failures**: 10 testes falhando (AiInsightsTest, AiIntegrationTest, AnalyticsPageTest, DocumentIntegrationTest) — não relacionados ao drill-down

---

## 2026-02-26 - US-105
- Adicionado property `drillFeatureId` com `#[Url(as: 'feature')]` em `⚡features.blade.php` e `⚡project-detail.blade.php`
- Computed property `drillFeature()` carrega Feature com tasks, project, timeEntries
- Métodos `enterDrill(int $featureId)` e `exitDrill()` para navegar entre modos
- Métodos `getDrillColumnTasks()` e `getDrillColumnTotal()` para tasks agrupadas por status
- Array `drillLimits` e `loadMoreDrill()` para lazy loading no drill mode
- Array `drillTaskStatuses` com [Backlog, Todo, Doing, Done] para colunas do drill
- Files changed: `⚡features.blade.php`, `⚡project-detail.blade.php`
- **Learnings for future iterations:**
  - Os dois componentes (features e project-detail) precisam das mesmas alterações de drill-down
  - Feature model já tem `tasks()` relationship e eager loading funciona com `with(['tasks.project', 'tasks.timeEntries'])`
  - Imports necessários: `TaskStatus`, `Task`, `Carbon`, `Url` (project-detail não tinha Url antes)
  - 82 testes relevantes continuam passando
---

## 2026-02-26 - US-108
- Implementado template condicional `@if($drillFeatureId && $this->drillFeature)` em ambos componentes
- Kanban de tasks no drill mode com 4 colunas: Backlog → Todo → Doing → Done
- Done column filtra por `completed_at` na semana corrente (reutilizando `getDrillColumnTasks`)
- Cards de task com: handle (grip-vertical), título, projeto, prioridade, estimativa, overdue, timer, session badge
- `wire:sort="handleTaskSort"` com `wire:sort:group="drill-tasks"` para drag-drop (handler será US-109)
- Lazy loading via `loadMoreDrill()` com `drillLimits` por coluna
- Done column colapsável com localStorage (`drill-done-collapsed` / `project-drill-done-collapsed`)
- Project-detail: no drill mode, não mostra header "Nova Feature" e badge de contagem mostra tasks
- Files changed: `⚡features.blade.php`, `⚡project-detail.blade.php`
- **Learnings for future iterations:**
  - Template de task card é similar ao kanban.blade.php mas sem `withCount('commits')` — tasks da feature vêm via collection, não query builder
  - No project-detail, o drill mode substitui todo o conteúdo do board tab panel (header + kanban)
  - wire:sort:group deve ser diferente do features kanban ("drill-tasks" vs grupo de features) para evitar conflito
  - `wire:key` dos drill tasks usa prefixo "drill-task-" para evitar conflito com outros componentes
---
